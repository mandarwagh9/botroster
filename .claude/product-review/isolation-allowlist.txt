# Containment words that have been read and are fine.
#
# CLAUDE.md: "Do not write documentation, comments or UI copy that implies isolation the project
# does not have." Gate G5 in scripts/review.sh finds every occurrence of sandbox / isolat* /
# virtual machine / container in README.md, docs/SPEC.md and the UI, and fails on any it cannot
# find here. This file is therefore a record of judgements, not a mute list: each row says why
# that occurrence is not an overclaim, and a reviewer should be able to disagree with the reason.
#
# Format:  <substring of the matching line> TAB <reason>
# Blank lines and lines starting with # are ignored. Match is on the whole "file:line:content"
# string, so a substring of the prose is the stable choice - line numbers move, sentences do not.
#
# When you legitimately change one of these sentences the gate fails, and the fix is to update
# the row. That upkeep is the price of the ratchet. Deleting a row you have not read is the one
# way to use this file wrongly.

# --- Denials. These lines exist to say the property is ABSENT, which is the behaviour we want. ---
a VM or a container. `fs.*` resolves against the workspace root	README.md states plainly that the guest is neither, then names what actually does the refusing. This is the sentence the gate exists to protect, not a violation of it.

# --- Descriptions of the REFERENCE product, not of OPENBOT. -------------------------------------
runs inside the sandboxed VM. What is **not** published	Describes xai-workspace-server inside Grok Bot's VM. Accurate about them, and the same sentence goes on to say the gap is what OPENBOT is.
the `devbox` sandbox	Describes a published Grok Bot profile, and the line itself opens "Status: design decision, not observed fact".

# --- The word naming something other than process containment. ----------------------------------
permissions / sandbox formats: which are **Claude Code compatible**	Names a configuration file format. PROVENANCE.md 1 records formats as interfaces adopted for compatibility.
Put a Bot's coat on a container	A DOM element in main.js. Nothing to do with process isolation.

# --- Properties OPENBOT genuinely has, or states as unbuilt future work. -------------------------
hypervisor-agnostic so a container today and a microVM later	Marked as the design of openbot-store, and CLAUDE.md's "What does not exist yet" says the hypervisor backend is absent. Reads as a plan, not a claim of today.
**Firecracker / Cloud Hypervisor microVM** | ✅ **target.** Real isolation	A ranked table of candidate runtimes. "target" is the column that says this is not built.
which is the boundary §11.2 and `openbot-guest/tests/isolation.rs`	Names the credential boundary and the test that enforces it. That boundary is real.
**Credential isolation under real conditions.**	The guest having no code path to the credential store is true today and enforced by isolation.rs. A different property from process containment, and the stronger one.
`crates/openbot-guest/tests/isolation.rs` walks the workspace's own dependency	Cites the enforcing test. Enforcement, not a claim.
